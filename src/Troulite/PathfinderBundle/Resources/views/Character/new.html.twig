{% extends '::base.html.twig' %}

{% block content -%}
    <div class="container">
        <h1>Character creation</h1>

        {{ include('@TroulitePathfinder/partials/ability_scores.html.twig') }}

        {{ form(form) }}

        <ul class="record_actions">
            <li>
                <a href="{{ path('characters') }}">
                    Back to the list
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
