{% extends '::base.html.twig' %}

{% block content -%}
    <h1>Character creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('characters') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
