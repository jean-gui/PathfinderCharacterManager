{% extends '::base.html.twig' %}

{% block content -%}
    <div class="container">
        <h1>Race creation</h1>

        {{ form(form) }}

        <ul class="record_actions">
            <li>
                <a href="{{ path('races') }}">
                    Back to the list
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
