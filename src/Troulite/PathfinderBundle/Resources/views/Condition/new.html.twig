{% extends '::base.html.twig' %}

{% block content -%}
    <h1>Condition creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('conditions') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
